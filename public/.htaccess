<IfModule mod_rewrite.c>
    Options -Multiviews
    RewriteEngine On
    RewriteBase /userlogin/public
    RewriteCond %{REQUEST/FILENAME} !-d
    RewriteCond %{REQUEST/FILENAME} !-f
    RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
</IfModule>